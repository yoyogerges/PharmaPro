services:
  postgres:
    image: postgres:16-alpine
    container_name: pharmapro-postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: pharmapro
      POSTGRES_PASSWORD: pharmapro123
      POSTGRES_DB: pharmapro
    ports:
      - "5433:5432"
    volumes:
      - postgres-data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U pharmapro -d pharmapro"]
      interval: 5s
      timeout: 5s
      retries: 10

  redis:
    image: redis:7-alpine
    container_name: pharmapro-redis
    restart: unless-stopped
    command: ["redis-server", "--appendonly", "yes"]
    ports:
      - "6380:6379"
    volumes:
      - redis-data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 5s
      retries: 10

volumes:
  postgres-data:
  redis-data: